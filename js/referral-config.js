'use strict';

// Fill these in after deploying referral-backend/Code.gs as a Google Apps
// Script web app (see referral-backend/README.md). Leave both blank to keep
// the referral system fully local-device-only — codes, the WhatsApp prefill,
// and the "show at venue" voucher all still work; only cross-device logging
// for the /partner-report.html dashboard needs this.
window.FEELBG_REFERRAL_LOG_ENDPOINT = '';
window.FEELBG_REFERRAL_DASHBOARD_KEY = '';
